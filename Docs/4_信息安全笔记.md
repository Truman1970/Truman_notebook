<!-- TOC -->

- [计算机硬件基础](#%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%A1%AC%E4%BB%B6%E5%9F%BA%E7%A1%80)
    - [中央处理器-CPU](#%E4%B8%AD%E5%A4%AE%E5%A4%84%E7%90%86%E5%99%A8-cpu)
    - [控制器-CU](#%E6%8E%A7%E5%88%B6%E5%99%A8-cu)
    - [运算器-ALU](#%E8%BF%90%E7%AE%97%E5%99%A8-alu)
    - [CPU中主要寄存器](#cpu%E4%B8%AD%E4%B8%BB%E8%A6%81%E5%AF%84%E5%AD%98%E5%99%A8)
    - [指令的寻址方式](#%E6%8C%87%E4%BB%A4%E7%9A%84%E5%AF%BB%E5%9D%80%E6%96%B9%E5%BC%8F)
    - [存储器](#%E5%AD%98%E5%82%A8%E5%99%A8)
    - [总线和接口](#%E6%80%BB%E7%BA%BF%E5%92%8C%E6%8E%A5%E5%8F%A3)
    - [外部设备](#%E5%A4%96%E9%83%A8%E8%AE%BE%E5%A4%87)
    - [冯诺依曼体系](#%E5%86%AF%E8%AF%BA%E4%BE%9D%E6%9B%BC%E4%BD%93%E7%B3%BB)
    - [计算机常用性能指标](#%E8%AE%A1%E7%AE%97%E6%9C%BA%E5%B8%B8%E7%94%A8%E6%80%A7%E8%83%BD%E6%8C%87%E6%A0%87)
- [计算机网络基础](#%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E5%9F%BA%E7%A1%80)
    - [计算机网络类别](#%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E7%B1%BB%E5%88%AB)
    - [常见网络拓扑结构](#%E5%B8%B8%E8%A7%81%E7%BD%91%E7%BB%9C%E6%8B%93%E6%89%91%E7%BB%93%E6%9E%84)
    - [OSI的体系结构](#osi%E7%9A%84%E4%BD%93%E7%B3%BB%E7%BB%93%E6%9E%84)
    - [TCP/IP体系结构（现在常用）](#tcpip%E4%BD%93%E7%B3%BB%E7%BB%93%E6%9E%84%E7%8E%B0%E5%9C%A8%E5%B8%B8%E7%94%A8)
    - [IPV4](#ipv4)
    - [IPV6](#ipv6)
    - [DNS](#dns)
- [对称密钥密码模型](#%E5%AF%B9%E7%A7%B0%E5%AF%86%E9%92%A5%E5%AF%86%E7%A0%81%E6%A8%A1%E5%9E%8B)

<!-- /TOC -->



## 计算机硬件基础

### 中央处理器-CPU
CPU基础功能：指令控制、操作控制、时间控制、数据加工。
CPU的组成：运算器、控制器、Cache。

### 控制器-CU
- 功能：指挥计算机各个部件协调一致的自动运行。
- 组成：程序计数器-PC、指令寄存器-IR、指令译码器、时序产生器、操作控制器。

### 运算器-ALU
- 功能：在控制器的指挥下，进行算数运算和逻辑运算。
- 组成：算数逻辑单元-ALU、通用寄存器、数据缓冲寄存器-DR、状态条件寄存器-PSW。

### CPU中主要寄存器
- 数据缓冲寄存器—DR：暂时存放ALU的运算结果、或由数据存储器读出的一个数据字，或来自外部接口的一个数据字。
- 指令寄存器-IR：保存当前正在执行的一条指令。
- 程序计数器-PC：存放下一条指令的地址，确定下一条指令。
- 数据地址寄存器—AR：保存当前CPU所访问的Cache存储器中单元的地址。
- 通用寄存器-R0-R3：当ALU执行算数运算或者逻辑运算时，为ALU提供一个工作区。
- 状态寄存器-PSW：保存由算数指令和逻辑指令运算或测试结果建立的各种条件代码。进位和借位保存在状态寄存器里，根据值判断是否由进位和借位。

> 构成运算器的部件中，最核心的是**算术和逻辑运算单元**


### 指令的寻址方式

指令格式：操作数+地址码

- 隐含寻址，指令中隐含操作数地址，一般来说，操作数存储在专用寄存器中。
- 立即寻址，操作数存储在指令中，不需要访问存储器，速度最快。
- 直接寻址，操作数地址存储在指令中，操作数存储在存储器中。
- 间接寻址，相比于直接寻址，两次寻址才取到操作数。
- 寄存器寻址，寄存器地址存放在指令中，操作数存储在寄存器中。
- 寄存器间接寻址，相比于寄存器寻址，两次寻址才取到操作数，操作数存储在存储器中。
- 偏移寻址，寄存器直接寻址 + 寄存器间接寻址。

> 获取操作数速度最快的是**立即寻址**。
> 操作数地址在某寄存器的寻址方式为**寄存器间接寻址**。

### 存储器

- 一个存储器中所有存储单元的总数称为它的容量。
- 静态存储器很快，断电信息丢失。
- 存储器分级：速度由快到慢，价格由高到低：寄存器、Cache、主存（RAM）、外存。


### 总线和接口

CPU和内存交互**按照传输信息**分类：数据总线、地址总线、控制总线。
- 数据总线
  - 用于传输数据信息。
  - 数据信息在数据总线上双向传输。
  - 数据总线的根数决定了通过该总线一次可以传送的信息量。
- 地址总线
  - 用来传输地址信息，地址信息在地址总线上单向传输。
  - 地址总线的根数决定了总线可以直接寻址的内存地址空间大小。
  - n根地址总线可以寻址的地址空间是2的n次方。
- 控制总线
  - 用于在主控设备和被控设备之间传输控制信号。

CPU和内存交互**按照所处位置**分类
- 片内总线
  - 发生在CPU内部。
  - CPU内部多个功能部件相连的数据传输通路。
  - CPU内部运算器、控制器以及寄存器之间通过片内总线传输数据。
- 系统总线
  - 发生在主机内部。
  - 又称为内总线，是CPU内存和各种接口电路之间的信息通路。
- 通信总线
  - 发生在主机外面。
  - 又称为外总线，是计算机和计算机之间或计算机和其他通信设备之间的信息传输通路，如电缆、网线、光纤等。

**接口**：临时存储信息的部件。
- 并行接口
  - 主要用于连接打印机或者扫描仪。
  - 并行传输受到线路长度限制，只适合10M以内的数据。
  - 并行接口适合高速、短距离通信。
- 串行接口
  - 用来连接Modem或其他串行通信设备。
  - 串行速度较慢，通信距离远，抗干扰强，连线成本低。
- 硬盘接口
  - IDE采用并行方式进行数据通信，接口速度快，价格低，兼容性强，每个IDE接口可以连接两个IDE设备，分别称为主设备和从设备。
  - SATA接口采用串行数据传输方式，可靠性高，抗干扰性强，支持热拔插，每个接口只能连接一个SATA设备，比较主流。
  - SCSI接口数据传输率高，CPU占用率低，除了连接硬盘外，还可以连接光驱、扫描仪、打印机设备等，较为常用。

> 断电后，存储在**RAM**的数据会丢失。  
> 解决主存和CUP之间速度不匹配的部件是**Cache**。  
> 总线宽度分为地址总线宽度和数据总线宽度，其中地址总线宽度决定了CPU能够使用多大容量的**主存储器**。  
> 若地址总线的宽度为32位，则最多允许**2^32b**访问的物理空间(如果没有标准答案，就选最接近的，如4GB，商家拿B代替b)。  

若内存地址区间为4000H~43FFH，每个存储单元可存储16位二进制数，该内存区域用4片存储器构成，则该内存所用的存储器芯片的容量是多少？
```shell
43FFH - 4000H + 1H = 4400H - 4000H = 400H = 2^10D
(2^10 * 2) / 4 = 2^9B
```

某计算机存储空间按字节编址，若某区域的地址空间为4A000H~4DFFFH，则该段内存区域的容量是多少？
```shell
4DFFFH - 4A000H + 1H = 4E000H -4A000H = 4000H = 2^14D
2^14 * 1 = 2^14B
```

思考：8G的内存，装32位的操作系统好？还是装64位的操作系统好？  
解：2^32b = 2^9MB ≈ 4GB，寻址空间只能是4GB。  

### 外部设备
- 输入设备：键盘、鼠标。
- 输出设备：显示器、打印机、绘图仪。
- 外部存储器：磁带、软盘、硬盘、光盘、U盘、移动硬盘。

磁盘存储设备
- 磁道的编址从外往内依次编号，最外的同心圆叫0磁道，最里面的同心圆叫n磁道。
- 每个盘面由许多磁道的同心圆组成。
- 扇区的编号可以是连续的，也可以间隔编号。

磁盘存储设备的技术指标
- 存储密度
  - 道密度，每一个磁盘上有多少磁道。
  - 位密度，单位磁道长度上能够记录二进制的位数，相同磁道位密度相同，0磁道位密度最小。
  - 面密度，整个磁盘面上可以存储多少信息，道密度和位密度的乘积。
- 存储容量
  - 格式化容量，写入文件系统之后的容量。
  - 非格式化容量，一般格式化容量是非格式化容量的60%-70%。
- 平均存取时间
  - 平均存取时间 = 平均找道时间 + 平均等待时间 + 数据传送时间
- 数据传输率
  - 单位时间内向主机传输数据的字节数（字节/秒）。

> 相比于温度、磁场、湿度，对磁盘影响最小的是**噪声**。  
> **错误**：同一个磁盘上每个磁道的**位密度**都是相同的。  
> **正确**：同一个磁盘上每个磁道的**容量**都是相同的。  
> **正确**：0磁道密度最小，n磁道密度密度最大。  
> **正确**：提高磁盘的转速一般不会减少平均寻道时间，会减少等待时间。  
> **正确**：磁盘的格式化容量一般都比非格式化容量小。  

### 冯诺依曼体系
两个思想
- 计算机五大部件：运算器、控制器、存储器、输入设备、输出设备。
- 存储程序原理：把要执行的指令和待处理的数据，按照顺序存储到计算机的内部存储器中，程序和数据以二进制的形式不加区别的存放，存放的位置由内存地址决定。


### 计算机常用性能指标
- 主频，CPU的时钟频率，决定计算机运算速度快不快。
- 字长，决定计算机的运算能力和存储单元的数据位数。
- 运算速度，每一秒可以执行多少条指令。
- 存储容量
- 内存寻址空间，和地址总线根数相关。
- 存储周期
- 总线带宽



## 计算机网络基础
### 计算机网络类别
- 按照不同作用范围
  - 广域网WAN，几十到几千公里，也成为远程网。
  - 城域网MAN，五到五十公里，一个城市或者几个大单位。
  - 局域网LAN，一公里左右，单位、公司、学校等。
  - 个人区域网PAN，家用。
- 按照不同使用者
  - 公用网，电信建立的公用网。
  - 专用网，军队、铁路、电路。
- 用来把用户接入到英特网的网络，ISP运营商，又叫本地接入网。


### 常见网络拓扑结构
- 总线型
  - 特点：通过一条电缆连接所有节点，一般支持30台计算机，过多计算机额外需要中继器。
  - 特点：半双工工作，一个时刻只能由一台计算机传输数据。
  - 优点：架设成本低。
  - 优点：容易安装。
  - 缺点：故障后果严重，任意一个节点出现故障，整个网络瘫痪。
  - 缺点：故障诊断困难。
  - 缺点：传输效率较低。
- 星型
  - 特点：中间有一个集线器（以前）或者交换机（现在），所有计算机通过中间的集线设备访问其他计算机。
  - 优点：故障诊断容易。
  - 优点：网络稳定性较好，故障隔离简单，一旦某一台计算机发生故障，不会影响整个网络。
  - 优点：可扩展型强。
  - 优点：提供网络传输速率容易。
  - 缺点：费用高，主要体现在中间的集线设备。
  - 缺点：布线麻烦。
  - 缺点：对中间节点依赖度较高。
- 树型
  - 特点：局域网常见的是树型结构，从根节点往下看，分别是核心层、汇聚层、接入层，公司常用。
  - 优点：易于网络扩展。
  - 缺点：故障诊断困难。
  - 缺点：对根设备依赖较高。
- 环型
  - 适用于大型网络。
  - 优点：网络可用性高。
  - 缺点：故障诊断难。
- 网状型
  - 缺点：线路成本高，控制复杂。
  - 优点：可靠性高，稳定性好。

> 单位网络拓扑结构一般严格保密，提高攻击难度。  
> 如果采用总线型网络拓扑结构，该网络传输信息的方式是**随机争用**。   
> 错误：**总线型拓扑结构**最大的缺点是：一旦中心节点故障，整个网络瘫痪。  
> 正确：在**令牌环网络拓扑结构**中，要发送信息的终端必须要获得令牌状态为0的令牌才能发送消息，并且在发送消息时将令牌状态置换为1。   

### OSI的体系结构
- 第一层，物理层。
  - 作用：透明传输**比特流**。处理信号通过介质的传输。
  - 主要设备：中继器（加强衰减信号，提高传输距离）、集线器（简单转发比特流，不进行碰撞检测）。
- 第二层，数据链路层。
  - 作用：透明传输**帧**信息。帧传输的时候会校验数据的顺序和完整性，保证数据正确的顺序、无差错和完整性。
  - 主要设备：网桥（接收、存储帧信息，查找转发表，然后传输帧信息）、二层交换机（连接设备、扩充网络范围）、网卡。
  - 常见协议：PPTP（单隧道协议）、L2TP（多隧道协议）、SLIP（串行线路网际协议）、PPP（点对点协议）。
- 第三层，网络层。
  - 作用：分组传输和路由选择。控制报文通过网络时的路由选择。
  - 主要设备：三层交换机（有路由功能）、路由器（通过路由表，实现网络通信）。
  - 常见协议：ARP（利用IP地址找MAC地址）、RARP（知道MAC地址找IP地址）、IP、ICMP（网际控制报文协议）、IGMP（网际组管理协议）。
- 第四层：传输层
  - 作用：端到端的连接。保证端到端的可靠性传输。
  - 常见协议：TCP（三次握手、可靠连接）、UDP（不可靠连接）。
- 第五层：会话层。
  - 作用：建立、管理和终止会话。
  - 常见协议：POP3、FTP、TFTP、HTTP、Telent、SMTP、SNMP、DHCP、DNS。
- 第六层：表示层。
  - 作用：数据的格式与表达、加密、压缩。
  - 常见协议：POP3、FTP、TFTP、HTTP、Telent、SMTP、SNMP、DHCP、DNS。
- 第七层：应用层。
  - 作用：为用户的应用进程提供服务。提供用户和网络的接口。
  - 常见协议：POP3、FTP、TFTP、HTTP、Telent、SMTP、SNMP、DHCP、DNS。

> 网桥的特点：使用网桥连接的网络在数据链路层以上采用相同或者兼容的协议。  
> 网桥的特点：使用网桥可以隔离冲突域。  
> 网桥的特点：使用网桥可以接收、存储、地址过滤及转发数据。  
> 错误：网桥处理接收到的数据**降低了**时延，提高了网络性能。  
> 正确：网桥处理接收到的数据**增加了**时延，因为多了碰撞检测。  

### TCP/IP体系结构（现在常用）
- 第一层，网络接口层，由物理层和数据链路层组成。
- 第二层，网际层（网络层）。
  - 常见协议：IP、ICMP（报告网络情况）、OSPF（路由选择协议）、ARP、RARP。
- 第三层，传输层。
  - 常见协议：TCP、UDP。
- 第四层，应用层，由会话层、表示层和应用层组成。
  - 常见协议（通过TCP传输）：HTTP、FTP、SMTP、Telnet。
  - 常见协议（通过UDP传输）：DNS、RIP、SNMP、DHCP。

### IPV4



| 类型 |                 网络号                 |               主机号               |           描述           |
| :--: | :------------------------------------: | :--------------------------------: | :----------------------: |
| A类  | 0开头，网络号8位，网络范围1-126(2^7-2) | 主机号24位，主机数16777214(2^24-2) | 大型网络，适合大型校园网 |
| B类  |  10开头，网络号16位，网络范围128-191   |  主机号16位，主机数65534(2^16-2)   |   中型网络，适合大公司   |
| C类  |  110开头，网络号24位，网络范围192-223  |    主机号8位，主机数254(2^8-2)     |     小型网络，小公司     |
| D类  |                  1110                  |              多播地址              |                          |
| E类  |                  1111                  |             保留以后用             |                          |

- IP地址结构：网络号+主机号，共32位二进制，4个字节组成。
- 网络号全0，代表本网络，不做网络号。
- 网络号全1，是环回地址，做测试使用，不做网络号。
- 主机号全0，代表本主机所在网络的地址，也就是某一个网段。
- 主机号全1，代表本网络所有主机。
- 主要掌握前三种，根据地址判断类型，根据网络位数求主机数。


**特殊IP地址**-**私有IP地址**-**专用IP地址**

| 类别 | IP地址范围                  | 网络号                | 网络数 |
| ---- | --------------------------- | --------------------- | ------ |
| A    | 10.0.0.0-10.255.255.255     | 10                    | 1      |
| B    | 172.16.0.0-172.31.255.255   | 172.16-172.31         | 16     |
| C    | 192.168.0.0-192.168.255.255 | 192.168.0-192.168.255 | 256    |


| 网络号 | 主机号         | 源地址 | 目的地址 | 代表的意思                                                   |
| ------ | -------------- | ------ | -------- | ------------------------------------------------------------ |
| 0      | 0              | 可用   | 不可用   | 0.0.0.0，代表本网络上的所有的主机                            |
| 全1    | 全1            | 不可用 | 可用     | 255.255.255.255，代表在本网络上进行广播                      |
| net-id | 全1            | 不可用 | 可用     | net-id表示某一网络，如192.168.1，代表对net-id所有主机进行广播 |
| 127    | 非全0或全1的数 | 可用   | 可用     | 用作本地软件环回测试使用 |                                   |




**子网划分**
- 网络号+子网号+主机号
- 为什么？
  - 划分为5类不合理，IP利用率低。
  - 给每一个物理网络分配网络号，会让路由表非常大，使查询效率降低。
  - 划分为5类不合理，不够灵活，如果需要一个网络，必须要到相关单位申请。
  - 提高安全性。
  - 内部管理更加方便。
- 子网掩码：代表子网号和主机号分别占多少位，全1代表网络号，全0代表主机号。
- 子网划分之后只对局域网内部可见，对外界来说，不需要知道，也不受影响。
- 实质：从主机号取若干位充当网络号，网络位会增加，主机位会减少。

> 多个子网，怎么知道某个网络属于哪个网段？  答案：子网掩码。  

**构造超网**
- 别称：地址聚合，网络聚合。
- 和子网划分相反，将原来的多个网络共同的部分进行聚合，网络位减少，主机位增加。
- 将多个网络合并成一个大的网络。

例1：将B类IP地址168.195.0.0划分成27个子网，子网掩码是多少？
```shell
2^n >= 27， 取 n = 5   
1010 1000 1100 0011 0000 0000 0000 0000  168.195.0.0 
1111 1111 1111 1111 1111 1000 0000 0000  255.255.248.0  
```

例2：将B类IP地址168.195.0.0划分成若干子网，每个子网有700太主机，子网掩码是多少？
```shell
2^n >= 700， 取 n = 10  
1010 1000 1100 0011 0000 0000 0000 0000  168.195.0.0  
1111 1111 1111 1111 1111 1100 0000 0000  255.255.252.0  
```

例3：如果某台主机IP是192.168.130.240，子网掩码是255.255.255.192，那么该主机所属网络是？
```shell
#初步观察，子网掩码的前24位都是1，所以不需要所有都换成二进制，只需要后八位换成二进制。
1111 0000  240  
1100 0000  192  
#上下做与运算，得出192。
#答案：192.168.130.192  
```

例4：在IPV4中，如果将一个C类网络划分子网，每个子网最少容纳50台主机，则最多可以划分的子网数是多少？
```shell
2^n >= 50， 取 n = 6  
8-6 = 2  2^2 = 4个  
```

> 212.54.22.0/24，代表网络位是24，对应的子网掩码是，255.255.255.0。

### IPV6

优点：
- 128位，更大地址空间。
- 路由更加高效。
- 更好的安全性。
- 移动性。
- 更好的QoS。
- 单播，一对一。
- 多播，一对多，发送给一组的主机。
- 任播，发给一组主机最短路径的主机。

> QoS（Quality of Service，服务质量），指一个网络能够利用各种基础技术，为指定的网络通信提供更好的服务能力，是网络的一种安全机制， 是用来解决网络延迟和阻塞等问题的一种技术。  

写法：
- 网络地址长度128位，使用16进制加冒号的表达方式，分成8段，每段用四个十六进制表示。
- 举例：FDEC:3CA1:010F:001A:0000:0000:0000:FFFF
- 缩写：FDEC:3CA1:10F:1A::FFFF
- 缩写规则1：两组以上的全0可以用`::`代替，但这种零压缩在地址中只能出现一次。
- 缩写规则2：前导的0可以省略。

> ::1，127个0 + 1个1，IPV6的简写方式。    
> mac地址举例：00-06-54-34-54-34  


### DNS
- 域名有树型的层次结构，从右往左域名等级降低，最右边是最顶级的。
- 常见域名代表的意思。
  - .com，表示公司企业
  - .net，表示网络服务机构
  - .org，表示非盈利性组织
  - .edu，表示教育机构
  - .gov，表示政府部门
  - .mil，表示军事部门
- www.bilibili.com，www代表网站服务。
- mail.sina.com.cn，mail代表邮箱服务。

**DNS解析**
DNS的递归解析，**本地DNS服务器提供最终解答**，返回的是所查询域名的IP。  
过程：DNS客户向本地域名服务器发起查询，如果没有查询到域名对应的IP地址，就充当DNS客户发起查询，直到查到后原路返回给DNS客户。   


| DNS客户发起查询 |
| :-------------: |
|       ↓↑        |
| 本地域名服务器  |
|       ↓↑        |
|  根域名服务器   |
|       ↓↑        |
| 顶级域名服务器  |
|       ↓↑        |
| 权限域名服务器  |



DNS的迭代解析，返回的是所查询域名的IP或下一个DNS的IP。  
过程：DNS客户发起查询，本地域名服务器如果有对应IP地址，则返回；如果没有，  
就向上级获取最初需要的IP地址或者上级DNS的IP地址，直到拿到最初需要的IP地址，返回给DNS客户。  


|           |      | DNS客户发起查询 |      |         |
| :-------: | ---- | :-------------: | ---- | :-----: |
|           |      |       ↓↑        |      |         |
|           |      | 本地域名服务器  |      |         |
|           | ↙↗   |       ↓↑        |   ↘↖   |       |
| 上一级DNS |      |   再上一级DNS   |      | 顶级DNS |



**Internet服务**
- www服务。url格式：协议://主机名.域名:端口号/目录及文件名。对于www服务来说，默认80端口不用写，其他端口都要写。
- 电子邮件服务，如edu@126.com。
- FTP服务，文件传输服务，需要TCP三次握手之后，然后登陆传输，匿名用户默认是anonymous。
- Telent服务，远程计算机连接，使用23端口。
- NewsGroup，新闻组，很少听说但用的很广，是完全交互式的超级电子论坛，outlook中就有新闻组，一般用于企业内部交流。
- Intranet计算，面对企业内部网络，Intranet安全技术包括防火墙、数据加密、身份认证和综合网络安全技术。

> 用户的电子邮箱是在**邮件服务器硬盘**上的一块专用的存储区。


**奇偶校验**
- 奇偶校验码的编码方式：有效信息 + 一个校验的二进制位
  - 原信息：1110000
  - 奇校验：11100000
  - 偶校验：11100001
- 奇校验：整个校验码（有效信息和校验位）中“1”的个数为奇数。
- 偶校验：整个校验码（有效信息和校验位）中“1”的个数为偶数。
- 特点：只能找错，不能纠错。
**循环冗余校验码CRC**
- 通过多项式进行编码，然后传输，多项式涉及一个除法叫模2除。
- 模2除的算法规则：相同为0，不同为1，本质是与或运算。
- 具体实践自行搜索。

**海明校验码**
- 原理：在有效信息位中加入几个校验位形成海明码，使码距比较均匀的拉大，并把海明码的每个二进制位分配到几个奇偶校验组中，当某一位出错后，就会引起有关的几个校验位的值发生变化，这不但可以发现错误，还可以指出错误的位置，这为自动纠错提供了依据。
- 只可以纠正一位错误数据。

| 7    | 6    | 5    | 4(2²) | 3    | 2(2¹) | 1(2⁰) | 位数 |
| ---- | ---- | ---- | ---- | ---- | ---- | ---- | ---- |
| S₄(1) | S₃(0) | S₂(1) |  | S₁(1) |  |  | 信息位 |
|      |      |      | r₂ |      | r₁ | r₀ | 校验位 |

重要公式：**2^r >= m+r+1**

> m代表传输数据的位数，r代表校验位的位数。发送数据4位时，r最小是3；发送数据5位时，r最小是4。上表中传输数据以1011举例。  









## 对称密钥密码模型
密码的安全需求
- confidentiality 保密性
- integrity 完整性
- availability 可用性

古典密码学要解决的问题：通信保密

三个古典密码系统
- 羊皮传书，两个一样的棍子
- 藏头诗，我爱秋香，又叫隐写术
- 凯撒密码，已知最早的替换密码

相关概念
- P: plaintext 明文
- C: ciphertext 密文
- K: key 密钥
- E: encipher(encrypt) 加密(加密算法)
- D: decipher(decrypt) 解密(解密算法)

这五要素构成一个密码系统，被称为对称密钥密码模型
```
E: P + K >>> C 
D: C + K >>> P 
```


